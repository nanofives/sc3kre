# SIMMISC.DLL — C0 slice 3 (25 largest of the remaining 1127), pass results

Every function below was read in full. Vocabulary from `SIMMISC.md` / `SIMMISC_CLUSTER1.md` / `SIMMISC_CLUSTER2.md` recurs unchanged: `FUN_1003222e(buf,msgId,grp)` = resource-string fetch, `FUN_10032266/FUN_100033ac/FUN_1003225b/FUN_10009645/FUN_10003d14` = GZ-string plumbing, `FUN_1002ad9b(str,&tmp,&DAT_delim)` = split-on-delimiter, `FUN_10029aef(a,b)` = string-equal matcher, `PTR_LAB_1003c434` = GZ-string vtable, `FUN_10003627` = string dtor, `FUN_1002ad75` = append-to-report, `FUN_1002b2cf` = set-string-into-widget, `FUN_1002d8ff` = number-formatter service, `FUN_10032100` = GZCOM root, `FUN_1002d87b` = message-server, `FUN_1002fe8d` = service/interface factory, `(**+0x1e4)(&a,&b,&c)` = read-calendar (day, _, year). Groups: `0x82e0074c` = query-tool/advisor table, `0x29541f4` = budget/dialog table, `0xc29a6083` = ordinance/neighbor resource-name group. RNG triad `FUN_1002f9e4`(seed)/`FUN_1002fc9f`(init)/`FUN_1002fcd0`(range) and float RNG `FUN_1002fd8d`/`FUN_1002fe45`.

## 1. Classification table

```csv
rva,subsystem,confidence,new_name,evidence
0x1002c6c6,S1,C2,sc3_msg_enum_ini_variables,"__thiscall(this,keyStr,callbackFn,ctx). Message-variable resolver method (mode-1/INI path). Guarded by this+0x19/this+0x18; acquires via (**this+0x3c), releases (**this+0xc). FUN_1002d1f3(this+0x1c,&h,&key) locates section, (**this+0x28 +0x30)(rec+0x24) maps to index bounded by (**this+0x28 +0x1c); iterates lines via (**this+0x40), skips ';' comments, breaks on '[', splits key=value on DAT_10049a98, builds a vector of 0x28-byte {key@+0,value@+0x14} records (FUN_1002cf3b), then invokes (*callbackFn)(rec+0, rec+0x14, ctx) for each; count=(end-begin)/0x28"
0x10031a5b,S1,C2,sc3_msg_enum_variables,"__thiscall(this,keyStr,callbackFn,ctx). Sibling of sc3_msg_resolve_token(0x10031764): FUN_10031dda(this) lazy-init, then dispatch on this+0xb4: 0=noop(ret 0), 1=delegate FUN_1002c6c6 (INI enum), 2=scan resource-embedded table. Mode-2 builds delimiter key from DAT_10049c6c prefix + keyStr + DAT_10049c68 suffix, tokenizes via (**this+0x78 +0x38)(&n,4), loops n tokens (FUN_10031d2f), when FUN_10029aef matches the delimiter it emits each subsequent token pair via (*callbackFn)(&val,&tok,ctx) until FUN_1002b201 sees DAT_10049c6c again"
0x1000eac4,S12,C2,sc3_query_build_report_xfa,"advisor text grp 0x82e0074c msg 0xfa/0xfb. formatter FUN_1002d8ff+0x94; root FUN_10032100+0x158 layer, value via (**+0x18); ftol(iVar5); two labelled rows appended via (**param_2+0x4c)(...,0x40000,0); tail FUN_1000c647 (location footer)"
0x100350fc,S12,C2,sc3_query_append_area_footer,"advisor FOOTER segment, deepest tail of the query chain (called as tail by 0x1000a5f0/0x1000c647/0x1000cc06...). __thiscall(this-8 obj). Gated by object flags (**this-8 +0x54)(8)||(0x10); formatter FUN_1002d8ff+0x94; root+0x15c layer, area=(**layer+0x58)(7) * (this+0xd8 byte width) * (this+0xd4 byte height); msg 0x31/0x32 grp 0x82e0074c; appended via (**param_2+0x4c)(...,0x10,0); tail FUN_100365a9"
0x1000bef5,S12,C2,sc3_query_build_report_x11c,"advisor text grp 0x82e0074c msg 0x11c/0x11d. root FUN_10032100+0x160 layer, value (**+0x14); ftol(&local_88); formatter FUN_1002d8ff+0x94 (**+0x40); tail FUN_1000a5f0"
0x10018957,S10,C2,sc3_budget_format_indexed_value,"(byte idx, int value, int outObj). Selects budget/dialog resource string by idx bucket grp 0x29541f4: idx<2->0x298, idx<4->0x296, idx<6->0x297, idx>5->ret; formats numeric `value` via FUN_1002d8ff+0x94 (**+0x3c)(value,value>>0x1f,outObj) and writes string into outObj+4 via FUN_10003996. [UNCERTAIN] the 0..5 index domain"
0x1002c429,S1,C2,sc3_msg_lookup_ini_variable,"__thiscall(this,sectionStr,keyStr,outStr). Single-key INI lookup (the mode-1 path of sc3_msg_resolve_token). Same guards/acquire/release as 0x1002c6c6; FUN_1002d1f3(this+0x1c) finds section, iterates lines skipping ';'/stopping at '[', splits on DAT_10049a98, matches key via FUN_10029aef(rec,keyStr); on match copies value into outStr via FUN_10003d14, returns 1; else 0"
0x1002811c,S1,C2,sc3_world_parse_valve_mapping_row,"TUNABLE-TABLE row parser (config visitor). __cdecl(keyStrObj,valStrObj). key id=FUN_100307d4((**keyStrObj+0x14)); parses value tokens split on DAT_10049714 then DAT_10049550; per token: count=FUN_100307d4(sub), float=(float)atol(sub)*_DAT_10041368; accumulates counts into int-vector (FUN_10029332/FUN_10029341) and running total; when a token equals valStrObj (FUN_10029aef) sets 'default' flag; builds record {id,0,0,0,count} and inserts into map DAT_10049f80 via FUN_100293a9. [UNCERTAIN] the exact table name (DAT_10049f80 is adjacent to WorldLayer EconomicPhases DAT_10049f70)"
0x1001abdd,S12,C2,sc3_query_value_by_key,"QUERY-KEY DISPATCH TABLE — implements the (**+0x60)(key) value-provider the advisor builders call. __thiscall(this,key). reads srcA=(**this+0xc +0x78)(), srcB=(**this+0x10 +0x10)(); looks key up in map this+0x28 (FUN_10009d86/FUN_1001b067), scale=rec+0x24 (default 100); big switch over ~50 GZ keys -> returns value/scale (0x15b4d1e,0x2f6e7e2,0xa0d07129,0xa2f917cf), -(value/scale)-1 (default LAB_1001ae4e, srcA or srcB per key), (-1-srcA/scale)-this+0x50/0x5c/0x60 (0xc2bf1dc5/0xe0d07233/0x42bf1e18), or fixed 0/1"
0x1000f1b9,S12,C2,sc3_query_build_random_tip_x134,"calendar-keyed advisor line. root+0x1e4 -> day,_,year; idx=__aullrem(day+year*0xc,10)=(day+year*12)%10; intro msg 0x134 then 0x135+idx grp 0x82e0074c; tail FUN_1000cc06"
0x1001b7ce,S1,C2,sc3_world_econphase_load_ranges,"__thiscall(this,f1..f9 out*, mode, _,_, applyScale char). Fills 9 float outputs (3 (base,lo,hi) triples) from _DAT_100411xx constant block selected by mode 0/1/2/3 (the 4 economic phases); RNG-floors f1 via FUN_1002fd8d(this+0x28,_DAT_10041160,_DAT_10041168) when below _DAT_10041170/_DAT_10041154; enforces min-ratios (_DAT_10041140), adds/subtracts bias _DAT_10041148; if applyScale, affine-maps all 9 by *_DAT_1004114c + _DAT_10041120. Called via vtable +0x80 from sc3_world_econphase_tick(0x1001b4bb)"
0x10013bef,S12,C2,sc3_query_build_random_tip_x174,"same template as 0x1000f1b9: (day+year*12)%10; intro msg 0x174 then 0x175+idx grp 0x82e0074c; tail FUN_1000cc06"
0x1001349e,S12,C2,sc3_query_build_random_tip_x15f,"calendar-keyed advisor line. root+0x1e4 -> year (local_2c); idx=year%9; intro msg 0x15f then 0x160+idx grp 0x82e0074c; tail FUN_1000cc06"
0x100116b9,S12,C2,sc3_query_build_random_tip_x14a,"calendar-keyed advisor line; idx=year%0xb; intro 0x14a then 0x14b+idx grp 0x82e0074c; tail FUN_1000cc06"
0x10012281,S12,C2,sc3_query_build_random_tip_x13f,"calendar-keyed advisor line; idx=year%10; intro 0x13f then 0x140+idx grp 0x82e0074c; tail FUN_1000c647"
0x10016678,S10,C2,sc3_neighbordeal_init_neighbors,"NEIGHBOR-CITY INITIALIZER w/ RNG. __fastcall(mgr). seeds RNG FUN_1002f9e4(&DAT_10049ec0)/FUN_1002fc9f; loop 4 dirs (0x54 stride from mgr+0x78): field-1=FUN_1002fcd0(13000)+2000, +0=4000000, +4 byte=1, rest zero; clears the 4 edge-availability bytes mgr+0xd4/0x80/0x17c/0x128 via map-bounds test (**mgr+0x14 +0x50)(x,y) using width mgr+0x1a0/height mgr+0x1a4; then two loops fill 4 slots at mgr+0x50 and 4 at mgr+0x64 (0x54 stride) with UNIQUE random resource ids (FUN_1002fcd0(0x24)+0x29 and +2, dedup via local_30) resolved to strings via FUN_1003222e(id,0xc29a6083)+FUN_100322ed, stored with FUN_10003d14"
0x10018cd5,S10,C2,sc3_neighbordeal_validate_active,"__fastcall(mgr). Per-tick validation of active deals. Locks (**mgr[0xb]+4); walks linked list mgr[9](+0x24); per deal-record rec(rec+0x38 age counter, incremented; rec+0x35 type byte 0..5; (**rec+0x1c)=category clamped<4): type0 checks slot mgr[cat*0x15+0x24]==0; type1/3 scan cell lists mgr[0x75]/mgr[0x72] verifying a matching-category connection still exists via FUN_10016f13 + (**mgr[6]+0x34)/(**mgr[7]+0x48); type2 checks mgr[cat*0x15+0x23]; type4/5 checks slot fields +0x20..+0x25 and (**mgr[4]+0x60)(0x302a); when invalid sets flag and cancels via (**mgr+0x80)(rec+0x35)"
0x10013928,S12,C2,sc3_query_build_report_x132,"advisor text grp 0x82e0074c. (**this+4 +0x84)(&pair) yields a dynamic (msgId,group); intro msg 0x132 then FUN_1003222e(pair.msg, pair.grp); appended via (**param_2+0x4c); tail FUN_1000c647"
0x10004793,infra,C2,sc3_rbtree_erase_rebalance,"STL std::_Tree::erase node removal + red-black rebalance. __cdecl(node,&root,&leftmost,&rightmost). splices node, fixes parent/child links, recolours ('\\0'=red '\\x01'=black) and rotates via FUN_10004688/FUN_100046c8 (left/right rotate); maintains leftmost/rightmost. Library helper (unmatched FidDb), not game logic"
0x1001b4bb,S1,C2,sc3_world_econphase_tick,"PER-TICK external-economy generator (world layer). __fastcall(this). reseeds RNG this[0x1e]=FUN_1002f9e4(this+0x10)/FUN_1002f9cf; reads coords via (**this[4]+0x200)+FUN_1002e0ce/FUN_1002e054 into this[5]/+0x16; phase=(**this+0x54)(); on phase change (**this+0x60)(mode,this[0x20]); calls (**this+0x80)(&f1..f9, phase,...) = sc3_world_econphase_load_ranges; draws this[0x21/0x22/0x23]=FUN_1002fe45(this+0x28, base+lo, base+hi) per triple, applies bias _DAT_10041148 and ratio clamps _DAT_10041144/_DAT_10041140"
0x10023d2a,S10,C2,sc3_budgetui_handle_command,"BUDGET-WINDOW COMMAND DISPATCH. __thiscall(this,_,cmdId). (**FUN_100320b1+0x34 +0x14)(1,0xc,0,0,0); switch cmdId: 0x32->broadcast msg 0x724a82e1 via FUN_1002d87b; 0x33->open panel via FUN_1002fe8d(0x4302b412,0x2302b3e5); 0x35->msg{0x724a82de,0x628d0c45,0xbbc}; 0x37->FUN_1002fe8d(0x62fa5742,0x62fa574b); 0x38->msg 0xbbd; 0xaf/0xb0/0xb1->msg 0x724a82dd sub-type {0x82a493a5,0x42a493c2,0x2a493dd}; tail FUN_10023fe1 (refresh summary)"
0x10031dda,S1,C2,sc3_msg_resolve_source_mode,"LAZY-INIT for the message-variable resolver (called first by 0x10031764/0x10031a5b). __fastcall(this). only runs when this+0xb4==0: if resource key this+100 non-empty (+0x1c) and FUN_10030f2d, opens config via this+0x78 (+0x70,+0xc(1,2,1)); extracts group/type from this+4 (FUN_10037395/FUN_10037418/FUN_1003742f), scans tokens (+0x38) matching via FUN_10029aef; on match caches value this+0xc0 and sets this+0xb4=2 (embedded-table mode); else sets this+0xb4=1 (INI-fallback mode)"
0x1000314a,S12,C2,sc3_aura_stamp_building_effect,"AURA STAMP — apply/remove a building's aura over its radius. __thiscall(this,mode,src). mode 0=add,1=subtract; id=(**src+0x88)(); looks up effect in map DAT_10049d38 (building-id -> {value@+0x14 (short), radius@+0x16 (byte)}), caching via FUN_10009d86/FUN_1000346f; center (**src+0xd0)(&box); expands box by radius clamped to map dims (**this+0xc)/(+0x10); loops tiles, (**this+0x5c +0x34)(x,y,&v) read aura, v+=value, (**+0x3c) write back. Reveals the per-building aura-effect table DAT_10049d38"
0x1002440a,S10,C2,sc3_budgetui_open_window,"budget/finance window open + saved-geometry restore. FUN_1002fe8d(0xa2a79fd0,0xa2a79fd1) creates window; sets style flags via (**+0x18) {1:1,2:0,0x10000:0,0x100:1,0x40:0,0x10:0}; child id 0x42e0509b (+0xec), size (**+0xc8)(0,0,DAT_100499f0,DAT_100499f4) with height DAT_100499f4=0x1cc when FUN_1002d8ff+0x20 screen-mode in {0xf,0x11,0x12,0x13,0x14}; restores saved pos DAT_10049f28(flag)/DAT_10049f2c/DAT_10049f30 validated vs screen rect, else centers (msg 0x624a8240 via FUN_1002d87b); sets pos (**+0xcc)(x,y)"
0x1000bc91,S12,C2,sc3_query_build_random_tip_x169,"calendar-keyed advisor line; root+0x1e4 -> year; idx=year%10; intro msg 0x169 then 0x16a+idx grp 0x82e0074c; appended via (**param_2+0x4c); tail FUN_1000ae77"
```

## 2. Notable findings (structural, highest value)

**A per-tick generator was found — `0x1001b4bb` = `sc3_world_econphase_tick` (S1).** `[CONFIRMED @0x1001b4bb]` This is the WorldLayer's external-economy driver the task asked to look for. Each call it reseeds the layer's RNG (`this+0x10` state, `this[0x1e]` seed), reads the current economic phase from `(**this+0x54)()`, pulls the phase's min/base/max float ranges from its own vtable slot `(**this+0x80)` = `0x1001b7ce`, and draws three values `this[0x21]/[0x22]/[0x23]` via the float RNG `FUN_1002fe45`, applying bias `_DAT_10041148` and the ratio clamps `_DAT_10041144/_DAT_10041140`. **`0x1001b7ce` = `sc3_world_econphase_load_ranges`** is its companion: mode `0..3` selects one of four `_DAT_100411xx` constant sets — the four **EconomicPhases** that `SIMMISC.md §S1` had only seen as an INI map. The `0..3` phase count and the paired vtable topology (`+0x54` get-phase / `+0x60` set / `+0x80` load-ranges on the same object) tie both functions to the WorldLayer.

**The aura-effect application path — `0x1000314a` = `sc3_aura_stamp_building_effect` (S12).** `[CONFIRMED @0x1000314a]` The add/remove counterpart to CLUSTER2's aura *propagation* tick (`0x10002d0b`). It looks a building up in a previously-unmapped table **`DAT_10049d38`** (building-id → `{value @+0x14 short, radius @+0x16 byte}`), then additively (`mode 0`) or subtractively (`mode 1`, on demolition) stamps `value` over a `radius`-expanded bounding box into the aura grid layer (`this+0x5c`, read `+0x34` / write `+0x3c`). This is the event callback fired on building place/remove.

**Neighbor-deals lifecycle grows by two (S10):**
- **`0x10016678` = `sc3_neighbordeal_init_neighbors`** — the new-city neighbor randomizer. Seeds the neighbor RNG `DAT_10049ec0` (same seed CLUSTER2 saw in `0x100170e6`), sets up the **4 compass-direction neighbors** (`0x54`-stride records from `mgr+0x50`/`mgr+0x64`), clears edge-availability bytes (`mgr+0xd4/0x80/0x17c/0x128`) against map bounds `mgr+0x1a0`(w)/`mgr+0x1a4`(h), and assigns each neighbor a random unique resource offer/want from group **`0xc29a6083`**.
- **`0x10018cd5` = `sc3_neighbordeal_validate_active`** — per-tick validation: walks the active-deal linked list (`mgr+0x24`), and per deal type (`rec+0x35`, `0..5`) verifies the underlying connection still exists (power/water tile scans via `FUN_10016f13`), cancelling stale deals through `(**mgr+0x80)`.

**Two message-id / command dispatch tables:**
- **`0x10023d2a` = `sc3_budgetui_handle_command`** — the budget window's command handler; a `switch` over button/menu ids (`0x32,0x33,0x35,0x37,0x38,0xaf–0xb1`) that broadcasts GZ messages through the message-server `FUN_1002d87b` and opens sub-panels through `FUN_1002fe8d`, then refreshes the summary (`FUN_10023fe1`, CLUSTER2).
- **`0x1001abdd` = `sc3_query_value_by_key`** — the concrete implementation of the `(**+0x60)(key)` value-provider that the entire advisor family calls; a large `switch` over ~50 GZ query keys returning `value/scale`, `-(value/scale)-1`, or member-offset-adjusted forms, with the per-key scale defaulting to `100`.

**The message-variable resolver class is now complete (S1).** Four methods of one class (`this+0xb4` = source mode, `this+0x1c` = section collection, `this+0x28` = section-index map, `this+0x78` = config stream, `this+100`/`this+4` = resource key, `this+0xc0` = cached value) all resolve here: `0x10031dda` (lazy source-mode init) → dispatched by `0x10031764` (single resolve, CLUSTER2) and `0x10031a5b` (enumerate), with `0x1002c429` (single INI lookup) and `0x1002c6c6` (INI enumerate) as the mode-1 backends. This is the news/message `{token}` substitution engine's data layer.

**A tunable-table reader — `0x1002811c` = `sc3_world_parse_valve_mapping_row` (S1).** A config-visitor callback that parses one `key=value` line into a record and inserts it into map **`DAT_10049f80`** (adjacent to WorldLayer's `EconomicPhases` map `DAT_10049f70`), building an int-vector of parsed sub-ids plus a `atol × _DAT_10041368` float. Matches the `ListOfWorldValveMapping` key named in `SIMMISC.md §S1` `[UNCERTAIN — exact table identity]`.

**Query/advisor family (grp `0x82e0074c`) grows by ~13** — six calendar-keyed "random advisor tip" builders (`0x1000f1b9, 0x10013bef, 0x1001349e, 0x100116b9, 0x10012281, 0x1000bc91`, picking one of N lines by `year%N` or `(day+year*12)%10`), four data-query builders (`0x1000eac4, 0x1000bef5, 0x10013928` + footer `0x100350fc`), and the shared area-footer tail `0x100350fc` (`sc3_query_append_area_footer`, msg `0x31/0x32`) that terminates the footer chain below CLUSTER2's `0x1000c647/0x1000cc06`.

## 3. Not determined / caveats

- **`0x10004793`** is STL (`std::_Tree::erase` rebalance), not game logic — it is in the `fun` backlog only because FidDb did not match it. Tagged `infra`; it should merge as library, not a SIMMISC subsystem.
- **`0x1002811c` table identity** `[UNCERTAIN]`: it inserts into `DAT_10049f80`; the arithmetic (int-vector + float via `_DAT_10041368`) fits `ListOfWorldValveMapping` from `SIMMISC.md §S1`, but the section name is a `.rdata` string absent from the export — needs a data read to confirm the key.
- **`0x1001abdd` key meanings** `[UNCERTAIN]`: the ~50 GZ query keys (`0x15b4d1e`, `0x42bf1e18`, `0xe0d07233`, …) select distinct return formulas, but each key's semantic (which quantity) is not derivable from the body; only the arithmetic and the `this+0x50/0x5c/0x60` member reads are certain.
- **`0x10018957` index domain** `[UNCERTAIN]`: maps `0..5 → {0x298,0x296,0x297}` in pairs; the caller's enum (matches CLUSTER2's open `0x100184e2` `0..5` question) is not in the body.
- **`_DAT_*` float literals** across `0x1001b7ce`/`0x1001b4bb`/`0x1002811c`/`0x100350fc` (`_DAT_100411xx`, `_DAT_10041368`) — the arithmetic and roles are confirmed, but the numeric values live in `.rdata` and are not in the text export (same standing open item as `_DAT_1003c644`).
- **Window-geometry globals** `DAT_100499f0/f4` (size) and `DAT_10049f28/2c/30` (saved position) in `0x1002440a` are named by mechanical role; their persisted numeric defaults (except height `0x1cc` for certain screen modes) are set elsewhere.
- **`sc3_query_value_by_key` (`0x1001abdd`) subsystem tag**: kept **S12** consistent with the CLUSTER1/2 advisor family it feeds, though strictly it is the cross-cutting data-view query layer, not aura-owned.

All 25 functions were read in full and are classified **C2** (body read, mechanically described, callees identified, named). None left at C0/C1. No C3/C4 claimed — the tick (`0x1001b4bb`), the aura stamp (`0x1000314a`), and the neighbor initializer (`0x10016678`) would each need runtime or a second witness to reach C3.
