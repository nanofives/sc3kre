# UNCERTAINTIES.md — SimCity 3000 RE

Every `[UNCERTAIN]` marker in source/notes MUST have a row here with the exact missing
evidence and a path to resolution. NO-GUESSING: a value whose meaning is unknown lives
here as a raw constant, not as a guessed name.

| ID | RVA / location | what is uncertain | evidence missing | path to resolution | status |
|----|----------------|-------------------|------------------|--------------------|--------|
| U-001 | `0x004a89f6` (sc3_html_init_tag_table) | which UI class renders the parsed HTML (consumer of `DAT_004fb170`) | xref chain from `DAT_004fb170` readers | **LEAD 2026-08-14: `0x004a3f0c` = `sc3_ui_init_html_window`, sets window title `HTML Window` @`0x004f9468`, gray bg (0xc0,0xc0,0xc0), lazy-loads resource key type `0x849b190a` grp `0x449b1912`. Not yet tied to `DAT_004fb170`.** Next: check whether `0x004a3f0c`'s window class reads `DAT_004fb170`; disambiguate newspaper/ticker HTML (S14) vs the CityExchange chat client | open |
| U-002 | `data.fez` FEZC meta record `+0x00` | role of `field0` (u32) in each 32-byte record | not size, not offset; ~0x106 on early entries | correlate with per-file uncompressed length once chunk-deflate is decoded | open |
| U-003 | `data.fez` compressed entries | ~~chunk framing of the payloads~~ | — | RESOLVED: single zlib stream, use `zlib.decompressobj()` not `zlib.decompress()`; 4321/4321 inflate clean; `.gvf`=`FE_GVF_BIN_0001` keyed table | resolved |
| U-004 | `0x0040b761` (sc3_boot_register_all_factories) | ~~direct caller~~ | — | RESOLVED (XrefProbe): referenced from `.rdata` init-table @`0x004cf668`; that table (~`0x004cf66c`) is walked by `FUN_00402e74`/`FUN_00403019` (module static-init iterators, seeded S-boot/C1) | resolved |
| U-005 | S2/S4/S5/S6 sim layers | which SC3U functions are goPowerLayer/goZoneLayer/goTrafficLayer/goValveLayer | **RESOLVED AT MODULE LEVEL 2026-08-14 — see `re/analysis/MODULE_MAP.md`. The sim layers are NOT in SC3U.exe: 29 `Apps\*.dll` export `GZDllGetGZCOMDirector` (GZCOM plugin modules, 6.2 MB total) and carry the class-name strings — `SIMRCI.DLL`=`SC3ValveLayer`/`SC3ZoneLayer`/`SC3ResLayer`/`SC3ComLayer`/`SC3IndLayer`; `SIMUTIL.DLL`+`SIMECO.DLL`=`SC3PowerLayer`/`SC3WaterLayer`; `SimTransit.dll`=`TrafficLayer`; `SIMMISC.DLL`=`SC3BudgetLayer`/`SC3WorldLayer`; `SIMGEOM.DLL`=`SC3BuildingLayer`; `SIMSERV.DLL`=`SC3FireLayer`; `SIMDSTR.DLL`=`SC3DisasterLayer`. Remaining uncertainty is per-function, not per-module: no DLL has been imported/decompiled yet.** Prior note: ADVANCED (SYSPAK.md): SYS.PAK cracked → `CitySim.ini [AgentTypes]` = 273 typed records `<id>=<name>:0x2026960B,0x41F56C4C,0x<classGZCLSID>` (full occupant/building taxonomy). But the go*Layer classes themselves are NOT in [AgentTypes] (only occupants + 4 `*LayerProxy` + `LayersAsAgents` 0x41F83776); core layers are ticked directly by the city sim loop | ANCHOR FOUND: GZCLSID constants appear in SC3U code (0x0040cb70 chain; FUN_00484c7a/FUN_00484b6d reference 0x2026960B/0x41F56C4C/0x41F836xx). Grep SC3U for a specific per-class GZCLSID (e.g. 0x41F83716=CoalPlant) to pin building/occupant classes; for the layers, live-Ghidra vtable xref | advanced |
| U-006 | occupant/building classes (S2/S3/S4/S6/S9/S11…) | ~~SC3U function per building/occupant type, via its 0x41F836xx GZCLSID~~ | — | **FALSIFIED 2026-08-14 (exhaustive byte scan of `original/SC3U.exe`, all 1,155,072 offsets, unaligned included): ZERO occurrences of any `0x41F836xx` dword; `0x41F56C4C` = 0 occurrences; `0x2026960B` = exactly 2, both code immediates inside `FUN_00484b6d` (file off 0x84b82) and `FUN_00484c7a` (0x84c80). The concrete per-type class GZCLSIDs exist ONLY in `SYS.PAK`/`CitySim.ini` data and are parsed at runtime — there is no per-building/occupant C++ class constant in the exe to grep.** Consequence: building/occupant types are fully data-driven; do NOT fan out a GZCLSID grind. | falsified — superseded by U-007 |
| U-007 | `0x00484b6d` / `0x00484c7a` / `0x00484b9a` / `0x00484b0a` / `0x00484ba5`, vtable `PTR_FUN_004da53c` | identity of the GZCOM "typed-key triple" object and the meaning of its two literal fields | ctor `FUN_00484b6d(this, param_1, param_2)` sets `+0x00=&PTR_FUN_004da53c`, `+0x04=0`, `+0x08=0x2026960b`, `+0x0c=param_2`, `+0x10=param_1`, then calls `FUN_004862e1`; getter `FUN_00484c7a` emits the triple `{0x2026960b, +0x0c, +0x10}` — same 3-field shape as a `CitySim.ini [AgentTypes]` value `0x2026960B,0x41F56C4C,0x41F836xx`. **21 caller functions** pass LITERAL pairs, e.g. `(0xa0, 0x209e6378)`, `(0x8e, 0x209e6378)`, `(0x8c, …)`, `(0x90, …)`, `(0x126, …)`, `(param_1, 0x42c1ed2d)`. Missing: what `0x209e6378`/`0x42c1ed2d` denote (NOT `0x41F56C4C`) and what the small ints index — they are NOT `[AgentTypes]` keys (checked: 140/142/144/160/294 absent; the 273 keys are round numbers 100…12114 plus 8 hex keys) | enumerate all 21 call sites + their literal `(index, group)` pairs; cluster by group id; correlate each caller's other strings/callees with an iOS named twin | **RESOLVED 2026-08-14 — the object is a GZCOM 3-tuple resource key `{type=0x2026960b, group=param_2, instance=param_1}`; `FUN_00484bef` resolves it to a localized `std::string`. All ~100 call sites are localized-text lookups. 23 functions merged into `functions.csv` (20 callers + the 3-function core family). See `re/analysis/RESOURCE_KEYS.md`.** Residual: the group ids themselves → U-008 |
| U-009 | `SIMUTIL.DLL` `0x1000c645` (GZCLSID `0x82bf0042`, 232 B) vs `0x10003275` (GZCLSID `0x20afdf44`, 764 B) | which of the two big standalone SIMUTIL classes is `SC3PowerLayer` and which is `SC3WaterLayer` | neither ctor calls the layer tunable loader `0x10004979` — it is reached only through a vtable slot the text export cannot resolve | **RESOLVED 2026-08-14 (`VtableProbe.java`, new): `0x10004979` occupies slot 5 (+0x14) of vtable `PTR_LAB_100205c0`, installed by ctor `0x10003275` = GZCLSID `0x20afdf44`, 764 B -> that class is `SC3PowerLayer`. Symmetrically `0x1000c8e2` is slot 5 (+0x14) of `PTR_FUN_10022944`, installed by `0x1000c645` = GZCLSID `0x82bf0042`, 232 B = `SC3WaterLayer`.** Residual: the grid flood-fill is still not located (folded into U-012) | resolved |
| U-010 | `SIMRCI.DLL` valve methods `0x1002efe8` / `0x1002f218` / `0x1002f4ed` | the SC3ValveLayer class: its ctor, vtable and GZCLSID | all valve methods are reached via vtable/data — no static caller edge; the id is one of the 36 unbound GZCLSIDs in the SIMRCI director list (`re/analysis/SIMRCI.md`) | **RESOLVED 2026-08-14 (`VtableProbe.java`): `sc3_valve_load_tuning` `0x1002dff3` = slot 5 (+0x14) of vtable `PTR_LAB_1004ce9c`; `sc3_valve_apply_effects` `0x1002efe8` = slot 3 (+0xc) of a second vtable `PTR_FUN_1004cee4`; BOTH installed by ctor `0x1002dede`, which is called by factory `0x100367a7` => `SC3ValveLayer` = GZCLSID `0x60a42f32`, `operator_new(0x58)` = 88 bytes.** The `EndOfMonth` `[iOS-HINT]` is still NOT confirmed — slot 3 of a secondary interface is not by itself evidence of monthly cadence | resolved |
| U-011 | `SIMUTIL.DLL` power-plant struct, offsets beyond `+0x20` | where per-tick output, base capacity and failure chance live | `0x10006ba6` proves only `+0x1c` MaxLifeSpan and `+0x20` DeclineAge; **the iOS `goPowerPlant` map `+0x48/+0x4c/+0x58/+0x5c/+0x60` is contradicted** — see `SIMUTIL_SIMTRANSIT.md` | **RESOLVED 2026-08-14 — full field map in `re/analysis/SIMUTIL_SIMTRANSIT.md`: output `+0x08`, base capacity `+0x0c`, breakdown u16 `+0x10`, date sub-object `+0x14`, MaxLifeSpan `+0x1c`, DeclineAge `+0x20`, bool `+0x24`. Derate `[CONFIRMED @0x100069fb]`: `out = cap - (age-declineAge)*cap/(maxLife-declineAge)`, 0 once age >= maxLife; age from `0x100120a1`. Breakdown `[CONFIRMED @0x10006c27]`: `rand(Min,MaxOverworkMonths) < u16@+0x10`, signalled by `0x10006c67` (message `0x434346e6`). Attributes load/save from EXEMPLAR keys `0x2351faf8..fb` (`0x100077a6`/`0x1000785c`). ZERO of the 5 iOS offsets transfer.** Residual: nothing in the plant cluster writes `+0x0c` non-zero — the capacity-populating write is layer-side and not yet found | resolved |
| U-008 | string-table group ids `0x209e6378` `0x41f2625` `0x29541f4` `0x830cdf29` `0x225872fe` `0x63de4715` `0x15e410c` `0x42c1ed2d` | which localized string table each group id denotes, and where the tables live | groups appear in `SC3U.exe` AND in `SIMUI.DLL`/`SIMMISC.DLL`/`SIMADV.DLL`/`SIMINIT.DLL`/`SCENARIO.DLL` (byte scan); no group id occurs in `SYS.PAK`/`CitySim.ini`; the string data itself is not in `SC3U.exe` | locate the string-table resource files under `Apps\Res\` and match a known instance — e.g. grp `0x29541f4` inst `0x2a6` = the sysinfo window title (`0x00436a94`), grp `0x209e6378` inst `0x6a4`-`0x6d6` = sysinfo labels (`0x00436f31`); **LOOKUP PATH FOUND 2026-08-14 (`re/analysis/S15_UI.md`): the resolver is `0x004862e1` (NOT `FUN_00484bef`, which only copies the cached result) — `(*(*stringManager+0x14))(triple, 0x69, out)` @`0x004862f1`. The manager is GZCOM service CLSID `0x801998e4` / IID `0x1995e7`, obtained from framework `DAT_004fab74` @`0x00406ec6`.** Remaining: that service is NOT implemented in `SC3U.exe` — closing U-008 means RE-ing whichever DLL registers class `0x801998e4` (candidates by group-id presence: `SIMUI.DLL`, `SIMMISC.DLL`, `SIMADV.DLL`, `SIMINIT.DLL`, `SCENARIO.DLL`). **CLOSED 2026-08-14 — see `re/analysis/formats/IXF_segment.md`. The tables are `.IXF` GZ database segments: `Apps\Res\Text\<LANGUAGE>\*.IXF` (7 languages), magic `0x80C381D7` (verified on disk: `d7 81 c3 80`), 20-byte index records `{type,group,instance,offset,size}`. Chain: SC3U `0x004862e1` -> GZResourceD director `0x10002d89` registers `0x801998e4` -> factory `0x100033b1` -> ctor `0x10011ea4` -> vtable `PTR_LAB_1001d92c` slot 5 (+0x14) = `sc3_gz_resolve_key_to_string` `0x10012c48` -> `sc3_gz_open_indexed_db` `0x1000ca78`. NOT in SYS.PAK. The 8 group ids are index `group` fields — parse an ENGLISH .IXF to name them all.** Residual: the selector `0x69` (105) and the stored constants `1000000`/`70000` are still unexplained (readers behind vtables) | resolved |
| U-012 | `SIMUTIL.DLL` `SC3PowerLayer` (ctor `0x10003275`, vtable `PTR_LAB_100205c0`) | where the power-grid connectivity computation lives, and whether it is a 1-bit raster flood-fill as on iOS | the class is now identified (U-009) but its Simulate/tick method has not been read; no `Bit1_*`-style raster or `SelectionGrow` cap-600 code was found in the first SIMUTIL pass | **RESOLVED 2026-08-14/15 — see `re/analysis/POWER_GRID.md`. YES, it is a bit-per-tile 4-neighbour flood-fill, done as masked dword dilation (32 tiles at a time), not a work queue. `sc3_power_distribute_region` `0x10004e57`: seed `0x1000b6c9` @`0x10004e93` -> grow `sc3_bitraster_grow_capped` `0x1000bbfc` @`0x10004ec4` -> drain against a byte-per-tile demand raster. Rasters: region `base+0x58`, conductive MASK `base+0x5c`, demand bytes `base+0x2dc`, all allocated in `0x10003904`. ⭐ THE iOS CAP OF 600 IS CONFIRMED SC3-SIDE: literal `0x258` @`0x10004ee2`. The kernel is generic — 8 other SIMUTIL functions reuse it.** Residual: who WRITES the mask `+0x5c` and the demand bytes `+0x2dc` (both only read so far), and the identity of layer `vtable+0x3c` slot 15, the per-tile mark-powered emitter | resolved |
| U-013 | `0x004077b5` (the command-line options method) | whether it is actually invoked at startup — i.e. whether `-r`/`-f`/`-w`/`-d`/`-l`/`-c`/`-p` are live switches or dead code | zero textual callers in all 9,727 bodies, but that is not evidence of dead code: the definitely-live `0x0040496d` and `0x00403565` also have zero textual callers (vtable-dispatched). It is a `__thiscall` method of the app class whose ctor is `0x00402e74` (touches `this+0x30`, `+0xfc/0xfe/0x100/0x101`, `+0x110`, `+0x168`, all initialized there). Missing: vtable contents at `0x004cf684`/`0x004cf66c`/`0x004cf630`/`0x004cf59c`/`0x004cf58c` (app), `0x004d6bf4+0x48` (framework), `0x004d7750` (cmdline) — `globals.csv` has no pointer-array dumps | two independent paths: (a) `VtableProbe.java`/raw pointer dump of those ranges, (b) **empirical — run the game with `-w -r800x600 -sound:off` and observe**. See `re/analysis/LAUNCH_CONTROL.md` §3d | **RESOLVED 2026-08-15 (empirical, `re/analysis/LAUNCH_CONTROL.md` §9.1): baseline with no `-r` = 800x600; `-r640x480` = 640x480. A switch that changes observable behaviour is a switch that is read, therefore `0x004077b5` executes at startup and the entire char-switch table (§3a) is live.** The underlying vtable-dump gap remains, tracked at `LAUNCH_CONTROL.md` §11 | resolved |
| U-014 | `LAB_004010c6` (unhandled-exception filter body, `0x004010c6`–`0x00401119`) | where `DAT_004fa4e0` (the `SC3U_stkdmp.txt` file HANDLE) is opened | the handle global is only ever *read* (`0x00401707` +11); the `CreateFileA` that fills it must sit inside `LAB_004010c6`, which Ghidra never carved into a function (`FUN_004010a6` is 13 bytes ending `0x004010b3`; next function is `FUN_0040111a`). Not verifiable from the text export | force-disassemble/create-function at `0x004010c6` in live Ghidra, or read the raw bytes. Low priority: the crash logger is confirmed always-armed regardless. See `LAUNCH_CONTROL.md` §7a | open |
| U-015 | debug-stream vtables `PTR_LAB_004d6b6c` / `004d6b54` / `004d6b28` / `004d6b10` / `004d6a90` / `004d6a8c` (assigned in `0x0045f79d`) | whether the "Gonzo Debug Window" show setter `0x0045fbcb` is reachable from inside the running game (hotkey / console command / menu) | `0x0045fbcb`, `0x0045fbff` and `0x0045f994` have zero direct callers in the export; all are vtable-dispatched and the vtable data is not dumped. No `RegisterHotKey` and no accelerator table exist in the binary | dump those pointer arrays with `VtableProbe.java`. Note: **moot for the test harness** — an injector can call `0x0045fbcb` directly via singleton `DAT_004fabd0`. See `LAUNCH_CONTROL.md` §7b | **RUNTIME EVIDENCE 2026-08-15 (`LAUNCH_CONTROL.md` §10i)**: with per-function detours on the whole family during a 20 s normal startup, `get_debug_stream` (`0x00405f1f`) = **1** hit while `debug_window_create` (`0x0045f994`) = **0**, `debug_window_show` (`0x0045fbcb`) = **0**, `debug_console_dispatch` (`0x0046036f`) = **0**. So the stream object IS constructed unconditionally (confirming the static reading) and its window is never created or shown on any normal startup path. This is not proof of unreachability - a hotkey/console path could exist later in a session - but it removes the startup path from consideration. Still moot for the harness: an injector can call `0x0045fbcb` directly via singleton `DAT_004fabd0` | narrowed |
| U-016 | `GZGraphicD.dll` (graphics service, GZCLSID `0xC416025C`), gfx vtable `+0xE4` | why `-w` does not produce a windowed game | the switch IS parsed and reaches gfx `vf+0xE4(1)` (`LAUNCH_CONTROL.md` §3a), yet runs 1-2 gave a `WS_POPUP|WS_VISIBLE` window at full desktop size (2048x1152) plus a display-mode change = DirectDraw exclusive fullscreen. Candidates, none tested: `0x0040586a` re-applying a persisted fullscreen flag via `vf+0xE4` AFTER `0x004077b5`; or windowed output unimplemented in this `GZGraphicD.dll` build | import `Apps\GZGraphicD.dll` into Ghidra and read the `vf+0xE4` implementation + `SetCooperativeLevel` call site; or hook it and log the call order. See `LAUNCH_CONTROL.md` §9.2 | **RESOLVED 2026-08-15 (`re/analysis/LAUNCH_CONTROL.md` §10a, from `re/ghidra_export_gzgraphicd/`). `-w` cannot work in this build, for two independent reasons.** (1) The fullscreen/windowed byte at graphics-object `+0x48` is unconditionally recomputed at Init: `0x100114b8` line 62 `*(bool *)(param_1+0x12) = DAT_1006cdac == 0;` and `DAT_1006cdac` has EXACTLY ONE reference in the whole DLL - that read (zero writers, verified by grep) - so it is zero-init BSS and the expression is always true => fullscreen. Line 146 re-forces it to 1 whenever the requested mode differs from the desktop, which a 16bpp request against a 32bpp desktop trips on its own. (2) Even the non-fullscreen style branch is still WS_POPUP: `FUN_100181D0` @`0x100181d0` yields `0x90000000` when IsFullScreen, else `0x80CA0000`; bit 0x4 (the only route to a non-popup value) is never set (flags word = 0x1B from `0x10017c83`, or 0 from the ctors). WS_OVERLAPPEDWINDOW is never produced. Consequence: windowed mode is a patch/hook job, not a switch | **RESOLVED, AND FIXED 2026-08-15 (`LAUNCH_CONTROL.md` §10c).** Working windowed mode = TWO in-memory patches, neither sufficient alone: (a) `GZGraphicD.dll+0x6CDAC` (VA `0x1006cdac`) `0 -> 1`, which inverts the Init line-62 override; (b) the four SC3U bpp immediates `16 -> 32`, so Init line 146 does not re-force fullscreen on the `current_bpp != requested_bpp` clause. Verified: external sampler shows the desktop stays 2560x1440@32 for the entire run (vs dropping to 800x600 without the patch); style is the windowed branch `0x80CA0000` (WS_POPUP|WS_CAPTION|WS_BORDER|WS_DLGFRAME|WS_SYSMENU|WS_MINIMIZEBOX), 806x629 = 800x600 + chrome, cooperative level `DDSCL_NORMAL`, and NO SetDisplayMode. A window titled "SimCity 3000" appears on the desktop. No style patch was needed. **Residual `[UNCERTAIN]`: whether the game RENDERS correctly windowed** - GDI capture of a DirectDraw surface is black, and runs were killed during startup. Do not call it fully working until confirmed | resolved + fixed |
| U-017 | effective startup resolution (`0x0040496d` tier byte `app+0xE8`, `0x0040586a` settings blob) | what supplies the 800x600 baseline | statically, `app+0xE8` is set to 0 by ctor `0x00402e74` and never written in SC3U.exe, which predicts 640x480 - but the game empirically comes up at **800x600** with no switches, and there is NO registry key under `...\Electronic Arts` and NO modified file in `Apps\` after three runs (so it is not saved user preference) | find the writer of `app+0xE8` in the plugin DLLs, or trace `0x0040586a`’s `vf+0x28` settings-blob source (likely SYS.PAK / .IXF packaged data). See `LAUNCH_CONTROL.md` §5 falsification note + §9.3 | open |
| U-018 | `IDirectDraw::SetDisplayMode` args inside `GZGraphicD.dll` | whether the hardcoded `bpp = 0x10` (16) request is actually applied, or virtualized/ignored on WDDM | the game requests 16bpp at all three `SetResolution` sites (`LAUNCH_CONTROL.md` §5), yet the primary display reported **32bpp on all three runs**. `Screen.BitsPerPixel` is a weak witness - it cannot distinguish "back buffer is 16bpp, compositor converts" from "request ignored end-to-end" | hook `SetDisplayMode` and log actual args + HRESULT, and query the primary surface pixel format. **Decides whether the "desktop color depth" work item is needed at all.** See `LAUNCH_CONTROL.md` §9.4 | **PARTIALLY RESOLVED 2026-08-15 (`LAUNCH_CONTROL.md` §10.2): the 16bpp request IS applied to the real display.** In-process `EnumDisplaySettingsA(ENUM_CURRENT_SETTINGS)` (DPI-independent) logs 2560x1440@32 -> 800x600@32 -> 800x600@16. The "same colour depth as desktop" work item is therefore REAL, not moot. Also corrects §9: the host desktop is 2560x1440, not 2048x1152 - `Screen.PrimaryScreen` was DPI-scaling by 1.25. Residual: which layer performs the mode set -> U-020 | **RESOLVED 2026-08-15 - and the earlier "partially resolved" verdict here was WRONG.** External sampling (`Win32_VideoController`, un-shimmed process) during a live run: 2560x1440@32 -> 800x600@**32** -> restored. **The real desktop never leaves 32bpp.** The 16bpp seen previously was an in-process artefact of the `DWM8And16BitMitigation` shim, which emulates the depth for the game while leaving the true mode alone. Confirmed by intervention: forcing the requested bpp 16->32 at all four SC3U immediates (RVA 0x4D27/0x4D43/0x5CF1/0x787B, verified pattern `C7 45 <disp> 10 00 00 00`) produced NO observable change, externally or internally. **Consequence: the "same colour depth as desktop" requirement is already satisfied and needs no code while the shim is active.** The real workflow disruption is the RESOLUTION change to 800x600 -> that is U-016 (windowed mode). See `re/analysis/LAUNCH_CONTROL.md` §10b | resolved |
| U-019 | audio path (`GZSOUNDD.DLL`, `UV.DLL`, audio svc `0x23CECBA0` `vf+0x14`) | whether `-sound:off` / `-music:off` actually silence output | the switches are confirmed parsed and call `Enable(false)` (`LAUNCH_CONTROL.md` §3b), but output was never measured; `DSOUND.dll` and `GZSOUNDD.DLL` load on every run regardless, and module presence is not evidence of playback | per-process audio-session peak meter (WASAPI `IAudioMeterInformation`) during a run with and without the switch. See `LAUNCH_CONTROL.md` §9.5 | open |
| U-020 | process-wide: `apphelp.dll` shimming `DDRAW.dll` | whether observed runtime behaviour reflects SC3U or SC3U-plus-Microsoft-compatibility-shim, and which layer actually sets the display mode | **CONFIRMED that a shim is active**: the IDirectDraw vtable is `DDRAW.dll+0x7D3C0` but every method points into `apphelp.dll` (`0x73F50000`-`0x73FFE000`); `SetCooperativeLevel(hwnd=NULL, DDSCL_FULLSCREEN)` returns DD_OK, which unshimmed DirectDraw would reject. **CONFIRMED that `SetDisplayMode` is never called** on either hooked vtable while the mode demonstrably goes 2560x1440@32 -> 800x600@32 -> 800x600@16, and NO game module imports `ChangeDisplaySettings*`. Missing: which layer performs the mode set | disable the compat shim (AppCompatFlags\Layers / sdb) and re-run `re/harness/sc3launch.exe`; if `SetDisplayMode` then appears on our hooks, the shim was swallowing it. Also hook `GetProcAddress` to catch a non-IAT DirectDraw entry. **Blocks C3/C4 behavioural confirmation generally** - runtime evidence must state whether the shim was active. See `re/analysis/LAUNCH_CONTROL.md` §10.1, §10.3 | **LARGELY RESOLVED 2026-08-15**: the shim is `DWM8And16BitMitigation`, registered for `Apps\SC3U.exe` in BOTH `HKCU` and `HKLM` AppCompatFlags\Layers (HKLM value prefixed `$`, i.e. installer-set - GOG ships it deliberately). It emulates 8/16-bit colour for the process while leaving the real display at desktop depth (proved in §10b by external sampling). It does NOT change the real colour depth. **Standing rule adopted: any runtime claim about display state must be measured from OUTSIDE the shimmed process** - two conclusions in `LAUNCH_CONTROL.md` were wrong from in-process measurement. Residual: what issues the real 800x600 resolution change, since `SetDisplayMode` (`0x10011f73` line 51, the only site) never fires in the observed window and no module imports `ChangeDisplaySettings*` | mostly resolved |
| U-021 | `GZGraphicD.dll` windowed presentation path (`FUN_10019877` clipper @`0x10019877`, primary creators `FUN_100199c0` @`0x100199c0` / `FUN_10019cc7` @`0x10019cc7`) | why the game renders BLACK when forced windowed (U-016 patch applied) | **CONFIRMED black by direct observation, not a capture artefact**: `PrintWindow(PW_RENDERFULLCONTENT)` at t+30s and t+55s of a 90s run gives identical frames, 4.4% non-black = the caption bar only. Window placement and mode-preservation DO work. Candidates, unseparated: (a) the windowed path needs an `IDirectDrawClipper` + `SetHWnd` + a screen-relative destination rect that the fullscreen path skips; (b) `FUN_10019cc7` requests a FLIP CHAIN (`ddsCaps=0x2218` PRIMARY|FLIP|COMPLEX, backbuffer=1) which DirectDraw rejects with DDERR_NOEXCLUSIVEMODE outside exclusive fullscreen; (c) the game never rendered in ANY configuration and the patch is innocent. **Evidence gap: rendering has NEVER been confirmed in any configuration, including stock fullscreen** - all runs were killed during startup. | (1) run the stock-fullscreen control with the new Blt/BltFast/Flip/Lock/Unlock counters in `sc3probe.dll` to establish whether the game draws at all; (2) static read of the clipper/present path. See `re/analysis/LAUNCH_CONTROL.md` §10c | **RESOLVED 2026-08-15 - the windowed patch is INNOCENT (`LAUNCH_CONTROL.md` §10d).** Across four runs (stock x2, `-bpp 32`, and `-windowed -bpp 32` incl. a 90 s run) the probe logs EXACTLY ONE `CreateSurface` each, always the DX capability probe from `FUN_10012BD6` (`ddsCaps=0x200`, `hwnd=NULL`, `DDSCL_NORMAL`). **No primary surface (0x2200/0x2218) is ever created in ANY configuration**; `FUN_1001250D`s real cooperative-level call and `FUN_10011F73`/SetDisplayMode never run. So the clipper / flip-chain / screen-relative-rect hypotheses are MOOT - execution never reaches them. Rendering was never happening, patched or stock. Superseded by U-022 | **REOPENED then RE-RESOLVED 2026-08-15 (`LAUNCH_CONTROL.md` §10e).** The earlier "patch is innocent" verdict was CIRCULAR: the CreateSurface counts came from runs whose own COM vtable hooks were preventing rendering. User reported the game renders fine launched normally, which falsified it. Bisection: renders with `-noinject`, with `-passive`, with delayed injection, with IAT-only (`-nocom`), and with `-nodd`; renders BLACK only when the IDirectDraw vtable is patched in place (that vtable is owned by the `apphelp` shim). **Harness rule: always run `-nocom`.** With the known-good harness, fullscreen renders but `-windowed` is still black => the windowed patch is NOT exonerated -> U-023 | resolved (instrument fault) |
| U-024 | ~~`SIMSPR.DLL` `DAT_1007276c`, bitmap vtable `+0xc8`~~ | ~~what `vt+0xc8` sets~~ | — | **RESOLVED 2026-08-15 — the original "+0xc8 is the colour key" premise was RIGHT, and my doubt was wrong.** `sc3gfx_image_adopt_block` `0x10001700` (GZGraphicD) calls the owner's `vt+0xc8` with the **u16 at block `+0x0c`**, i.e. the per-sprite colour key `[CONFIRMED @0x10001700]`. The `0x10012e20` site that passes the writer-less global `DAT_1007276c` is a separate default/reset call, not the per-sprite path. Lesson: "one call site passes a zero global" does not generalise to "| **FINAL ROOT CAUSE 2026-08-15 (`LAUNCH_CONTROL.md` §10k).** Same engine surface: fullscreen 800x600 **16bpp = 7500/7500 LIT**, windowed 800x600 **32bpp = 0/7500 LIT**. `FUN_10019273` writes an explicit DDPIXELFORMAT only for 32/8bpp; at 16bpp it writes none, so surfaces INHERIT the display mode. Fullscreen changes the mode to 16bpp; windowed never changes the mode, so on a 32bpp desktop surfaces come out 32bpp - and the software blitter is 16bpp-first (`0x1000ce4c`/`0x1000cf31`/`0x1000d054` gate on `this+0x10 != 0x10`; `0x10014c05` has 8->16 and 32->16 converters but no 32bpp draw path). **So the `-bpp 32` patch that windowed REQUIRED to survive Init was itself disabling all drawing - the two patches are mutually exclusive.** Confirming experiment: neutralise the Init re-force (`0x100117d6` -> nop x4) and patch `FUN_10019273` to request explicit 16bpp 5-6-5 (`0x100192fb 74 4C -> 74 1F`, `0x1001931c ..41 -> ..40`, `0x1001932a` constants) => **the game aborts ~2.4 s in via the graceful AbortiveQuit path, no crash dump: DirectDraw will not create a 16bpp surface on a 32bpp display.** **Verdict: windowed is not achievable by patching on a 32bpp desktop.** It is obsolete rather than unfinished - it would have worked on the 16bpp desktops common in 2000. A fix needs a pixel-format conversion layer, i.e. a DirectDraw wrapper (DDrawCompat/dgVoodoo2). The present pipeline itself is proven working (§10j) | resolved - environmental, not patchable |
| U-024-old | `SIMSPR.DLL` `DAT_1007276c`, bitmap vtable `+0xc8` (call site `0x10012e20`) | what the "colour key" call at bitmap `vt+0xc8` actually sets | **the original hypothesis that the sprite colour key comes from bitmap `+0xc8` is now DOUBTFUL.** `0x10012e20` is `CALL [EAX+0xc8]` with the single argument `DAT_1007276c` `[CONFIRMED @0x10012e20`, raw disasm]. `DAT_1007276c` sits in `.data`, reads **0x00000000** in the file on disk (`pe_read.py`), and has **exactly ONE reference in the whole module — that read** (grepped all 3,310 SIMSPR bodies; zero writers). Same shape as the `GZGraphicD DAT_1006cdac` case in U-016: zero-init BSS with no writer. So `vt+0xc8` is being passed 0. Meanwhile the REAL per-sprite colour key is at offset `+0x0c` of the decompressed block, proven over 62,552 records (`formats/QFS.md`) | dump the `+0xc8` implementation once the bitmap class is identified (see U-023); or find a writer of `DAT_1007276c` in another module via a cross-module byte scan. **Not blocking** — the sprite format is fully decoded without it | **PROGRESS 2026-08-15 (`LAUNCH_CONTROL.md` §10g), still open.** §10f misidentified the present caller as `FUN_10016ba4` (nearest-preceding-function error; it is 58 bytes and ends at 0x10016bde, and traces 0 hits in BOTH modes). The real present path is at **`0x10016bf1`, in a region Ghidra never carved into a function** - hand-disassembled: `cmp byte [eax+0x15],cl / je 0x10016c0c / mov eax,[eax+0x44] / call [edx+0x38]`. `this+0x15` is the present gate, written by `FUN_10015e3d` from the flipping flag, which windowed forces to 0 => the present call is skipped every frame. **Intervention `sc3launch -present` patches `GZGraphicD+0x16BFE` `74 0C`->`90 90`: the client area goes from BLACK to WHITE, but still no game content.** `FUN_10019be8` (Blt) and `FUN_10019a77` both stay at 0 hits, so the windowed `[edx+0x38]` is a present implementation not among the 14 traced functions | next: at runtime dump the vtable of the object at `this+0x44` (probe can read it without patching) to identify `[edx+0x38]` in windowed mode, then find why it presents a blank surface | **RESOLVED 2026-08-15 - ROOT CAUSE COMPLETE (`LAUNCH_CONTROL.md` §10h).** Runtime vtable dump of the object at `this+0x44`: fullscreen present = vtable `GZGraphicD+0x1F5C0` slot 14 = `FUN_10019a77` (flip a real chain); windowed present = vtable `GZGraphicD+0x1F628` slot 14 = `FUN_10018dd9` = `this[1]->Flip(NULL,DDFLIP_WAIT)` on the PLAIN primary, failure discarded. **Decisive: detouring it to use the game`s own Blt (`FUN_10019be8`, direction 0 => dest=this+4, src=this+8) revealed `this+8 == NULL` on every call - the windowed device NEVER allocates a render target.** Fullscreen gets `this+8` from `GetAttachedSurface` on the `FUN_10019cc7` flip chain; windowed `FUN_100199c0` creates only the primary. Full chain: (1) flipping off => gate `this+0x15` closed => present skipped; (2) force gate => Flip on a plain primary, always fails silently; (3) substitute Blt => no source surface exists. **Verdict: completing windowed mode requires constructing a render target and re-pointing the engine draw path at it - engine work, not a patch.** Recommendation: use a DirectDraw wrapper (DDrawCompat/dgVoodoo2) if windowed output is needed, and declare it as a fidelity caveat like U-020 | resolved - not fixable by patching |
| U-023 | ~~`SIMSPR.DLL` IID `0x0487534f`~~ | ~~which class implements it; the meaning of span-block `a`, `b`, flags bit `0x8000`~~ | — | **RESOLVED 2026-08-15 — see `re/analysis/formats/QFS.md`.** Found by **byte-scanning the module binaries for the IID immediate** after grep over the text export failed in both hex and decimal (the decompiler drops these pushes and Ghidra never carved the functions). `FUN_1001daf5` `0x1001dc72` shows `CreateInstance(clsid=0xa487535d, iid=0x487534f, &out)`, so the class is **GZCLSID `0xa487535d` in `GZGraphicD.dll`** — registered by `0x100011ef`, factory `0x100012b5` (`new(0x38)`, returns +4), ctor `0x10001484`, IID vtable `PTR_LAB_1001e328`, slot 3 = `sc3gfx_image_adopt_block` `0x10001700`, slot 5 = the **encoder** `sc3gfx_image_encode_span_block` `0x100017de`. The encoder names the last three fields: `+0x08` = a hardcoded literal `4`; `+0x0a` = the **pixel-format id** from the surface descriptor at `vt+0x50` (whose `+4` is the bit depth, tested `== 0x10`); flags bit `0x8000` = **"this span contains no colour-key pixel"** (fully opaque fast path). | resolved |
| U-023-old | `SIMSPR.DLL` IID `0x0487534f`, consumer call `0x1001df74` | which class implements GZCOM interface `0x0487534f`, whose vtable slot 3 (`+0xc`) receives the decompressed span-sprite block | the call is `QueryInterface(0x487534f)` then `iface->vt[3](block, 1)` `[CONFIRMED @0x1001df5d-0x1001df74`, raw disasm via `re/scripts/DumpDisasm.java`; the decompiler loses this tail]. **No decompiled body in ANY of the 31 module exports contains a comparison against `0x487534f`** (grepped for `== / != / case`), so the QueryInterface that answers it is table-driven or otherwise non-textual. 15 call sites exist across SIMSPR + SIMUI, all consumers | `VtableProbe.java` on the SIMSPR classes that register a sprite/bitmap CLSID; or dump the vtables of the objects passed as `param_1` to `0x1001de49` (reached from `sc3spr_provider_make_bitmap` `0x10012d91`). **NOT blocking the format** — the span-sprite layout was confirmed from data (62,552/62,552) and renders correctly; this is needed only to name the header fields `a` (always 4), `b` (7 or 5) and flags bit `0x8000` | open |
| U-022 | SC3U startup, after `CreateWindowExA` (~770-980 ms) and `ShowWindow`, before primary-surface creation | where the game stalls: it makes NO further graphics calls for the following ~89 s | driver enumeration DOES complete (4 `DirectDrawCreate` calls: `FUN_100120F9` enumerate, `FUN_100121C4` per-driver, `FUN_100125C8` live object), so the stall is after enumeration and before `FUN_100199c0`/`FUN_10019cc7`. `GZGraphicD` swallows every DirectDraw HRESULT silently (no logging, no message box, one diagnostic string in the whole DLL), so a failure would leave no trace | **FIRST, cheapest step: does SimCity 3000 render on this machine when launched NORMALLY, with no injector?** If no, the fault is environmental (data files / GZCOM plugin load / the shim) and the harness is not implicated; if yes, injection itself is suspect. Then: hook the GZCOM plugin load path (`GZDllGetGZCOMDirector`, SC3U `0x0048a575`) and `FUN_100114b8` Init to see how far it gets. See `re/analysis/LAUNCH_CONTROL.md` §10d | **VOID 2026-08-15** - premise retracted. The game does NOT stall: it renders normally with `-noinject`/`-passive`/`-nocom`. The apparent stall was the COM vtable hooks breaking DirectDraw (§10e) | void |
| U-023 | `GZGraphicD.dll` windowed presentation: `FUN_10019877` @`0x10019877` (clipper), `FUN_10019be8` @`0x10019be8` (the only Blt), `FUN_100185f5` @`0x100185f5` (only ClientToScreen) | why the client area is BLACK when forced windowed, with the known-good harness (`-windowed -bpp 32 -nocom`) | window placement + desktop mode-preservation work (desktop stays 2560x1440@32); fullscreen with the SAME hook set renders fine, so the windowed patch itself is responsible. Candidates: (1) no clipper is attached to the primary - `FUN_10019877` is the ONLY CreateClipper/SetHWnd/SetClipper site and NEITHER primary creator (`FUN_100199c0`/`FUN_10019cc7`) calls it, and DirectDraw refuses a windowed blit to the primary without one; (2) no screen-relative destination rect - `FUN_10019be8` blits with the caller rect verbatim, no ClientToScreen/GetWindowRect offset; (3) every DirectDraw HRESULT is silently discarded so all of this fails invisibly. Working hypothesis: **SC3U`s windowed path was never finished/shipped** | resolve the vtables (`0x1001f054`/`0x1001f0ac`/`0x1001f104`/`0x1001f628`) to find who calls `FUN_10019877` and which primary creator runs; then IAT-hook to force a clipper + SetHWnd after primary creation. See `re/analysis/LAUNCH_CONTROL.md` §10e | **PROGRESS 2026-08-15, still open (`LAUNCH_CONTROL.md` §10e.1-10e.2).** Vtables recovered by reading `Apps\GZGraphicD.dll` `.rdata` directly: `0x1001ECE0` slot 55 = `+0xDC` = `FUN_10010ED1` (**confirms SC3U SetResolution slot**); `0x1001F628` is a derived device class whose vtable ENDS after slot 22, where it overrides the primary creator with `FUN_100199C0` (`ddsCaps=0x2200`, no flip chain); clipper `FUN_10019877` is slot 20 (+0x50) in both device classes. **`FUN_10019CC7` (0x2218 flip chain) is in NO vtable => that hypothesis is DEAD.** TWO INTERVENTIONS FAILED: (a) attaching a clipper by COM calls only (GetGDISurface/CreateClipper/SetHWnd/SetClipper, all DD_OK) leaves the window black; (b) the "no screen-relative rect" prediction is FALSIFIED - a DPI-aware full-desktop capture shows no frame at the screen top-left either. The game is not presenting AT ALL when forced windowed; the failure is upstream of clipper and blit geometry. | next: inline 5-byte detours on GZGraphicD FUNCTIONS (safe - unlike patching the apphelp-owned COM vtable) at `FUN_100114b8` Init, `FUN_1001250d`, `FUN_100199c0`, `FUN_10019be8`, to trace how far the windowed path actually gets | **RESOLVED 2026-08-15 - ROOT CAUSE FOUND (`LAUNCH_CONTROL.md` §10f).** Inline detour tracer on 13 GZGraphicD functions, fullscreen vs windowed, 20 s each. **The engine`s only present path is `FUN_10019a77` @`0x10019a77` (Flip on the surface at this+8), and Flip requires a flip chain.** Fullscreen: primary via `FUN_10019cc7` (ddsCaps=0x2218 PRIMARY|FLIP|COMPLEX) then 1050 flips in 10 s (~105 fps) -> renders. Windowed: plain primary via `FUN_100199c0` (ddsCaps=0x2200, no back buffer), `FUN_10019a77` called **0** times, `FUN_10019be8` (Blt) called **0** times -> frames are rendered into offscreen surfaces and never presented. Everything else in the windowed path is correct: it picks device class `0x1001F628`, sets DDSCL_NORMAL, creates 48 offscreen surfaces, and **calls its own clipper** (so the `-clipper` intervention was redundant, not wrong). **Verdict: SC3U`s windowed presentation was never finished - the game shipped fullscreen-only.** Fix is possible but is real engineering -> U-024 | resolved |
| U-024 | `FUN_10019a77` @`0x10019a77` (present/Flip), `FUN_10019be8` @`0x10019be8` (Blt) | making windowed mode actually present frames | root cause is known (U-023): the frame loop only Flips, and the windowed primary has no flip chain. The pieces needed already exist - `FUN_10019be8` blits `dest=this+4, src=this+8` when its `param_3 == 0`, which is exactly the required shape; `FUN_100185f5` @`0x100185f5` already computes a ClientToScreen rect (correctly gated on !IsFullScreen) | detour `FUN_10019a77` (or its caller) so that when windowed it performs the Blt instead of the Flip, with a ClientToScreen-offset destination rect. The detour tracer built for U-023 (`sc3launch -trace`, verified 5/6-byte prologue steal) is the vehicle. See `LAUNCH_CONTROL.md` §10f | open |
| U-024-RETRACTION | windowed presentation, whole subsystem | — | **THE U-023/U-024 VERDICT "SC3U's windowed presentation was never finished - the game shipped fullscreen-only" IS FALSIFIED.** Windowed mode renders. Confirmed visually 2026-08-16: `sc3launch -nocom -windowed -origin` produces a real captioned "SimCity 3000" window with the intro video playing in the client area, native, no ddraw wrapper. The failure that drove ten rounds of §10 was **placement**, not a missing present path: the destination is the DirectDraw primary surface, so the window must lie inside the PRIMARY display. Four placements, same build: `0,0` renders; `-3,-26` black; `-2560,-1` (monitor 3) black; `2560,155` (monitor 2) black with sound and cursor. Positive coordinates are not sufficient - containment in the primary display is the discriminator. **Methodological cause of the error: every "is it rendering?" test had been run with the window at a position that could not render, so a true negative on placement was read as a negative on the whole mechanism.** See `LAUNCH_CONTROL.md` §12 | — | **resolved - windowed mode works** |
| U-025 | `GZGraphicD.dll` device bpp field `this+0x10`, set by `FUN_10009efb` @`0x10009efb`; blit dispatcher `FUN_10014894` @`0x10014894` | why the intro video draws at HALF width in windowed mode | the video fills 320x480 logical of the client, right-hand half black, colours otherwise correct. Half width with correct colour = a bytes-per-pixel mismatch: a row's byte count computed at 16bpp (640 px x 2 = 1280 B) written into a 32bpp surface fills only 320 px. **The SC3U-side mode request is NOT the cause**: `-bpp 32` patches all 4 sites and `GetDeviceCaps(BITSPIXEL)` returns 32, picture unchanged. So the wrong depth lives on the GZGraphicD side | read `device+0x10` and the destination surface's `ddpfPixelFormat.dwRGBBitCount` in the same frame at runtime; `FUN_10014894` already branches on `this+0x10 == srcfmt+4`, so log both operands of that compare. The detour tracer (`-trace`) is the vehicle | open |
| U-026 | primary-surface placement constraint | whether windowed output can be moved off the primary display at all | monitor 2 at `2560,155` renders black while sound and the in-game cursor work, so the engine is running and only the pixels are lost. Unknown whether this is a hard DirectDraw primary-surface limit or something re-targetable | test with the secondary display temporarily set as the Windows primary: if the game then renders there, the constraint is "primary display" and is a Windows-side setting, not an engine limit | open |
| U-024-RETRACTION-WITHDRAWN | windowed presentation | — | **THE ROW ABOVE (U-024-RETRACTION) IS ITSELF WRONG AND IS WITHDRAWN. U-023/U-024's original root cause STANDS: the windowed device never allocates a render target, so the game's frame loop presents nothing.** Corrected observation 2026-08-16: in windowed mode the intro VIDEO plays in the client area, then the screen goes black and the menu never appears. Those are two different paths - the movie player blits itself to the primary surface, while the game frame loop presents via Flip on a device whose `this+8` is NULL (§10h). **Video rendering is not evidence that the game renders.** I inferred "windowed mode works" from the video alone and retracted a correct conclusion; the user's report that the menu is black is what falsified it. What survives from that row is ONLY the placement constraint, which is independently established: the window must lie inside the primary display or even the video does not appear (4 placements tested). See `LAUNCH_CONTROL.md` §12 as amended | remaining work is unchanged from U-024: construct a render target and re-point the engine draw path at it | **U-024 reopened - still open** |
| U-025-AMENDED | `FUN_10009efb` @`0x10009efb` | why the intro video draws at half width | device init args are `[800, 600, 4, 16]` - **bpp 16** `[CONFIRMED @0x10009efb]`, and this is the same in both modes, so 16bpp is the game's normal configuration and not a windowed anomaly. `-bpp 32` does NOT reach this call: the GZGraphicD call site at `0x1001626B` (inside `FUN_1001611b`) receives 16 with and without the switch, so **every §10b conclusion drawn from `-bpp 32` was testing nothing**. The bpp value arrives via `param_1[4]`, not a literal, so forcing it needs an argument-rewriting detour rather than a byte patch. Causal link between bpp and the half width is NOT established | affects only the intro movie, which is cosmetic next to U-024; park it until the render target exists | open, low priority |
| U-027 | present `0x10016BF1`, guard field `this+0x1C` | what drives the present after the intro movie ends | the present has NO direct callers - it is reached only via the vtable pointer at `0x1001F2C0` `[CONFIRMED - DLL byte scan]`. Its only identified driver is the intro movie player `FUN_00429f95` (SC3U, ret addrs `0x0042A207`/`0x0042A232`). In fullscreen something calls it ~105x/s; that caller is unidentified. Two gates decode exactly: guard `+0x1C` tested first (`jne` at `0x10016BF9`), gate `+0x15` second (`je` at `0x10016BFE`). **`-present` only ever patched the second, so every §10f-§10i conclusion about "forcing the present" was clearing a branch the code never reached.** Bypassing the guard as well HANGS the game at ~1.15 s (log stops, image frozen, sound continues) => `+0x1C` is a busy/re-entrancy lock, not a mode flag - do not patch it | find the fullscreen caller: trace with the fixed (idempotent) tracer in fullscreen and log `ret` for every PresentFn hit, then diff against windowed. See `LAUNCH_CONTROL.md` §13 | open |
| U-025-ANCHORED | intro movie half width | why the 640x480 movie displays 320 wide | `FUN_00429f95` sets the movie size via `vt+0x44 (0x280, 0x1e0)` = **640x480** `[CONFIRMED @0x00429f95]`, and it renders 320 wide - a factor of **exactly 2** on a known source width, not an estimate. Combined with device bpp = 16 `[CONFIRMED @0x10009efb]` this is consistent with a row byte-count computed at 16bpp landing in a 32bpp destination, but the destination's actual bit depth has still not been read in the same frame | Lock the movie's destination surface and read `ddpfPixelFormat.dwRGBBitCount`; if 32, the mismatch is proven | open |
| U-028 | intro movie player `FUN_00429f95` @`0x00429f95`, suspend counter `0x10016BDE` | why the renderer is never resumed after the intro movie | `this+0x1C` is a suspend counter (`add [ecx+0x1C],eax` with a clamp at zero), and the present at `0x10016BF1` skips the frame while it is non-zero. A full 40 s windowed run records **exactly one call: delta=+1 from SC3U+0x2A298, and no decrement ever** `[CONFIRMED @0x10016BDE]`. `0x0042A298` sits in the intro movie player at the line `vt+0x38 (1)` = Suspend(+1). This accounts for the black client, the surviving sound, and the present being polled ~3M/s while exiting at the guard. **Corrects §13.3: the field is a counter, not a lock - nopping the branch forces presents during a suspended state, hence the hang.** | find the movie's stop/cleanup path and why its `Resume(-1)` does not run windowed; meanwhile `-resume` clears the counter directly (BUILT BUT NEVER EXECUTED - the `SC3PROBE_RESUME` env read was missing, so every run labelled `-resume` before that fix was a plain windowed run) | open |
| U-029 (PARTIAL 2026-08-16: the map dimension N IS readable from the file after all - section {0x406b1196, 0x80ab8ab0} is frame(8)+N*N+8, N=isqrt(size-16), agreeing with the zone blob's 3*N*N+tail in 59/59; a stride-coherence test confirms both are real rasters. ATTEMPT 8 (2026-08-16) falsified the "three N*N planes" reading: only the FIRST N*N is a raster (stride coherence .796 at N); the middle 2*N*N has coherence ~.01 at every stride, uses all 256 byte values and is 1.1% zeros vs plane 0's 62.5% - high-entropy packed data, not a map layer and not QFS. The zone GRAMMAR for the middle + the 900+6k tail is still open. ATTEMPT 8b FOUND IT: the grammar starts at N*N and parses cleanly in 59/59 - u32 c1 (0 or 3) with keys ALWAYS 3000/5000/8000, two 23-byte blocks, then c2 and c3 which are ZERO in every shipped file. It consumes 58 or 82 bytes and STOPS. All eight earlier attempts failed for ONE reason: they required the grammar to consume to the section end, and it never does. Still open: the ~2*N*N high-entropy region AFTER the grammar, which the loader as read does not account for. RESOLVED 2026-08-17 by reading the saver end to end: the recorded grammar was MISSING TWO WRITES - a third 23-byte block at this+0x3c and a DELEGATED write (*(this+0x268))->vt+4(param_1, stream). That delegated write is 2*N*N + 795..1323 bytes, i.e. the whole high-entropy region is one sub-object serialising itself. Verified: the last 24 bytes are six small u32s in 59/59. The sub-object is at object+0x27c, vtable PTR_FUN_1004d2bc, ctor-installed at param_1[0x9f], fields init by FUN_10031267. RESOLVED 2026-08-17 on live Ghidra: PTR_FUN_1004d2bc slot 1 = FUN_1004361d, which writes a u32 count then count x u16, one per tile, in REVERSE order - the 2*N*N is a 16-bit-per-tile plane. Verified at a PREDICTED offset: the u32 at (end - 36 - 2*N*N - 4) equals N*N in 59/59. Also SETTLED: the +0x188/+0x18c discrepancy - VtableProbe shows the saver lives on the subobject at object+0x14 (vtable 1004d198 slot 10) and the registrar on object+0x10 (vtable 1004d1e0 slot 22), multiple inheritance 4 bytes apart, so BOTH readings addressed the same fields at object+0x19c/+0x1a0. Still open: (a) RESOLVED - the u16 plane is a PERMUTATION: all N*N values pairwise distinct in 59/59, and exactly the permutation of 0..65535 in the 47 N=256 files. It is a traversal order, not map content, which explains the uniform histogram and zero coherence. Still open: the keys 3000/5000/8000; (b) a 1000-1270 byte gap between the grammar and the u16 count that sits INSIDE the sub-object header - the visible header writes account for only ~26 bytes, so I inferred vt+0x98 must be non-scalar. FALSIFIED 2026-08-17: VtableDump on PTR_FUN_1001cb34 gives slot 38 (+0x98) = FUN_1000c1d6, the SAME function as +0x88, i.e. a plain u32 write. The header really is ~26 bytes and the gap (1,221 bytes in Berlin) is UNEXPLAINED again. NOT resolved - a wrong turn, recorded: I claimed the 0x17 blocks are 23 elements of 4 bytes. FALSIFIED by disassembly (100321bb PUSH 0x17; PUSH EDX; CALL [EAX+0x84]) and by VtableDump showing all three stream flavours share +0x84 = FUN_1000c1ad = Write(ptr,len) in BYTES. The blocks ARE 23 bytes. Solved by asking which element size S makes the distance from the grammar end to the independently-known u16 count CONSTANT: S=4 gives 118 in all 59 files, and 118 = 92 (a third such block at this+0x3c) + 26 (the sub-object header, exactly u8+u32+bool+5*u32). The zone section is now accounted for end to end with no unexplained bytes. c2/c3 are NOT zero under the corrected block size - the earlier "always 0" was an artefact of parsing 46 bytes where 184 belong. WHAT THE S=4 SWEEP ACTUALLY SHOWED: attributing 184 bytes to the block term makes the distance to the u16 count constant in 59/59, while the true blocks are 46 - so there are ~138 CONSTANT unexplained bytes between the two blocks and the c2 count. That is the open item. NOTE: two inferences in a row about this gap were wrong (vt+0x98, then the block size), both from taking a number that made the arithmetic close and calling it a cause. Measure the cause. 2026-08-17 STRUCTURAL FINDING: the saver and loader are NOT mirrors at the tail. They agree up to the third 23-byte block, then the saver delegates to (this+0x268)->vt+4 while the loader instead calls (this-0x14)->vt+0xc and works through an object piVar1 (vt+0x10, vt+0x34, vt+0x10, vt+0xc). The loader NEVER touches this+0x268. So cross-confirming the grammar "from both directions" proves nothing past block C - which is exactly where the 138 bytes and the u16 region are. RESOLVED: piVar1 is NOT a stream - it is the base subobject (this-0x14), i.e. the grid. The loader block is a POST-LOAD RECOUNT: nested loop over vt+0xc rows x vt+0x10 cols, fetching each tile via vt+0x34(y,x,&v) and histogramming v<0x17 into a 23-entry u32 array at this+0x3c. It reads NOTHING from the stream. So the saver's delegated write to this+0x268 has NO loader counterpart: the u16 permutation and its ~138-byte header are WRITTEN BUT NEVER READ BACK - regenerable state a toolkit does not need. Bonus: the guard v<0x17 independently confirms tile values are slot indices 0..22 and that 0x16 is in range. CONTRADICTION RESOLVED 2026-08-17 - it was MY READING ERROR. The loader DOES read the sub-object, at 0x10031c85:166, in the ELSE arm of an if/else: (*(code *)**(undefined4 **)(this + 0x268))(param_1, local_c) = SLOT 0 of that object, and VtableDump already showed slot 0 = FUN_1004350e (read) beside slot 1 = FUN_1004361d (write). A mirror pair after all. I missed it because my call-listing regex required an explicit "+ 0xNN" slot offset and a slot-0 call has none. WITHDRAWN: "saver and loader are not mirrors at the tail", "the u16 plane is never read back", "regenerable state a toolkit does not need". The permutation finding itself stands (it came from the bytes). NEW and surviving: the tail is CONDITIONAL - one arm reads the sub-object, the other recomputes the 23-entry histogram at this+0x3c from the loaded grid; what selects between them is UNCERTAIN and is the next read. Superseded detail follows: base reader FUN_1001b4b4 consumes exactly rowCount*rowBytes and nothing else, so the loader totals ~N*N+129 while the saver totals the full section. The loader would read its six trailing u32s at N*N+105, inside the sub-object region - the wrong bytes. The game loads fine, so AT LEAST ONE RECORDED FACT IS WRONG. Candidates: (1) the base read is not N*N (but it cannot be 3*N*N - the u16 permutation is at the END, so 3*N*N lands inside it); (2) the c1 list at N*N is coincidence (against: keys 3000/5000/8000 in all 25 developed files, exact parse); (3) the loader's six u32 reads are not the mirror of the saver's six writes (plausible - the tail is already proven asymmetric); (4) something consumes the region that the decompilation does not show. DO NOT resolve by arithmetic - four inference attempts have failed. Track the stream object's position across the whole loader, or breakpoint under a debugger. Note the bytes just before the count hold N-1 twice (0xff/0xbf/0x7f for N=256/192/128), i.e. grid extents.) | SC3ZoneLayer section, saver `0x100320e7` / loader `0x10031c85` | the internal grammar of the zone instance-0 section (3*N^2 + 900+6k bytes) | the flat grammar `u32 c1; c1x{u32,u32}; 23B; 23B; u32 c2; c2x{u8,u8,u32}; u32 c3; c3x{u8,u8,u32}` is confirmed from BOTH saver and loader and is provably NOT in the bytes: attempt 7 ran it forward from every start AND **backward from the known section end** on the corrected base-0 window, with the loader's own bounds - **0 fits in 59/59 files**. What IS measured: the first `N^2` bytes are a 1-byte-per-tile raster (H=2.13, 14 distinct values in Berlin; H=0.58, 9 in Farmsville), `N in {128,192,256}`, and the tail is 900 bytes in all 34 unplayed files with every variant a multiple of 6. | the `if (cVar != '\0')` guards in the saver can skip whole writes - model them, or breakpoint the loader under a debugger. **Do NOT sweep the grammar again; seven attempts have failed.** | OPEN |
| U-028-RETRACTED | suspend counter `0x10016BDE` | — | **U-028's claim that the renderer is never resumed is FALSIFIED.** With the tracer and the movie trace table loaded in the same run: `delta=+1` at movie start (`SC3U+0x2A298`) and `delta=-1` at movie stop (`SC3U+0x2A35F`), counter back to 0 `[CONFIRMED @0x10016BDE]`. Suspend/resume are correctly paired and `FUN_0042a31a` works. The earlier "1 hit, no decrement" was one run whose movie ended outside the measured window, generalised into a root cause - the same error as the §12 retraction. **The persistent blocker remains `gate(+0x15)=0` (the flipping flag, §10h), not the suspend counter.** What survives: `this+0x1C` is a counter, not a lock, so the do-not-nop rule from §13.3 stands | — | resolved - not the cause |
| U-029 (PARTIAL 2026-08-16: the map dimension N IS readable from the file after all - section {0x406b1196, 0x80ab8ab0} is frame(8)+N*N+8, N=isqrt(size-16), agreeing with the zone blob's 3*N*N+tail in 59/59; a stride-coherence test confirms both are real rasters. ATTEMPT 8 (2026-08-16) falsified the "three N*N planes" reading: only the FIRST N*N is a raster (stride coherence .796 at N); the middle 2*N*N has coherence ~.01 at every stride, uses all 256 byte values and is 1.1% zeros vs plane 0's 62.5% - high-entropy packed data, not a map layer and not QFS. The zone GRAMMAR for the middle + the 900+6k tail is still open. ATTEMPT 8b FOUND IT: the grammar starts at N*N and parses cleanly in 59/59 - u32 c1 (0 or 3) with keys ALWAYS 3000/5000/8000, two 23-byte blocks, then c2 and c3 which are ZERO in every shipped file. It consumes 58 or 82 bytes and STOPS. All eight earlier attempts failed for ONE reason: they required the grammar to consume to the section end, and it never does. Still open: the ~2*N*N high-entropy region AFTER the grammar, which the loader as read does not account for. RESOLVED 2026-08-17 by reading the saver end to end: the recorded grammar was MISSING TWO WRITES - a third 23-byte block at this+0x3c and a DELEGATED write (*(this+0x268))->vt+4(param_1, stream). That delegated write is 2*N*N + 795..1323 bytes, i.e. the whole high-entropy region is one sub-object serialising itself. Verified: the last 24 bytes are six small u32s in 59/59. The sub-object is at object+0x27c, vtable PTR_FUN_1004d2bc, ctor-installed at param_1[0x9f], fields init by FUN_10031267. RESOLVED 2026-08-17 on live Ghidra: PTR_FUN_1004d2bc slot 1 = FUN_1004361d, which writes a u32 count then count x u16, one per tile, in REVERSE order - the 2*N*N is a 16-bit-per-tile plane. Verified at a PREDICTED offset: the u32 at (end - 36 - 2*N*N - 4) equals N*N in 59/59. Also SETTLED: the +0x188/+0x18c discrepancy - VtableProbe shows the saver lives on the subobject at object+0x14 (vtable 1004d198 slot 10) and the registrar on object+0x10 (vtable 1004d1e0 slot 22), multiple inheritance 4 bytes apart, so BOTH readings addressed the same fields at object+0x19c/+0x1a0. Still open: (a) RESOLVED - the u16 plane is a PERMUTATION: all N*N values pairwise distinct in 59/59, and exactly the permutation of 0..65535 in the 47 N=256 files. It is a traversal order, not map content, which explains the uniform histogram and zero coherence. Still open: the keys 3000/5000/8000; (b) a 1000-1270 byte gap between the grammar and the u16 count that sits INSIDE the sub-object header - the visible header writes account for only ~26 bytes, so I inferred vt+0x98 must be non-scalar. FALSIFIED 2026-08-17: VtableDump on PTR_FUN_1001cb34 gives slot 38 (+0x98) = FUN_1000c1d6, the SAME function as +0x88, i.e. a plain u32 write. The header really is ~26 bytes and the gap (1,221 bytes in Berlin) is UNEXPLAINED again. NOT resolved - a wrong turn, recorded: I claimed the 0x17 blocks are 23 elements of 4 bytes. FALSIFIED by disassembly (100321bb PUSH 0x17; PUSH EDX; CALL [EAX+0x84]) and by VtableDump showing all three stream flavours share +0x84 = FUN_1000c1ad = Write(ptr,len) in BYTES. The blocks ARE 23 bytes. Solved by asking which element size S makes the distance from the grammar end to the independently-known u16 count CONSTANT: S=4 gives 118 in all 59 files, and 118 = 92 (a third such block at this+0x3c) + 26 (the sub-object header, exactly u8+u32+bool+5*u32). The zone section is now accounted for end to end with no unexplained bytes. c2/c3 are NOT zero under the corrected block size - the earlier "always 0" was an artefact of parsing 46 bytes where 184 belong. WHAT THE S=4 SWEEP ACTUALLY SHOWED: attributing 184 bytes to the block term makes the distance to the u16 count constant in 59/59, while the true blocks are 46 - so there are ~138 CONSTANT unexplained bytes between the two blocks and the c2 count. That is the open item. NOTE: two inferences in a row about this gap were wrong (vt+0x98, then the block size), both from taking a number that made the arithmetic close and calling it a cause. Measure the cause. 2026-08-17 STRUCTURAL FINDING: the saver and loader are NOT mirrors at the tail. They agree up to the third 23-byte block, then the saver delegates to (this+0x268)->vt+4 while the loader instead calls (this-0x14)->vt+0xc and works through an object piVar1 (vt+0x10, vt+0x34, vt+0x10, vt+0xc). The loader NEVER touches this+0x268. So cross-confirming the grammar "from both directions" proves nothing past block C - which is exactly where the 138 bytes and the u16 region are. RESOLVED: piVar1 is NOT a stream - it is the base subobject (this-0x14), i.e. the grid. The loader block is a POST-LOAD RECOUNT: nested loop over vt+0xc rows x vt+0x10 cols, fetching each tile via vt+0x34(y,x,&v) and histogramming v<0x17 into a 23-entry u32 array at this+0x3c. It reads NOTHING from the stream. So the saver's delegated write to this+0x268 has NO loader counterpart: the u16 permutation and its ~138-byte header are WRITTEN BUT NEVER READ BACK - regenerable state a toolkit does not need. Bonus: the guard v<0x17 independently confirms tile values are slot indices 0..22 and that 0x16 is in range. CONTRADICTION RESOLVED 2026-08-17 - it was MY READING ERROR. The loader DOES read the sub-object, at 0x10031c85:166, in the ELSE arm of an if/else: (*(code *)**(undefined4 **)(this + 0x268))(param_1, local_c) = SLOT 0 of that object, and VtableDump already showed slot 0 = FUN_1004350e (read) beside slot 1 = FUN_1004361d (write). A mirror pair after all. I missed it because my call-listing regex required an explicit "+ 0xNN" slot offset and a slot-0 call has none. WITHDRAWN: "saver and loader are not mirrors at the tail", "the u16 plane is never read back", "regenerable state a toolkit does not need". The permutation finding itself stands (it came from the bytes). NEW and surviving: the tail is CONDITIONAL - one arm reads the sub-object, the other recomputes the 23-entry histogram at this+0x3c from the loaded grid; what selects between them is UNCERTAIN and is the next read. Superseded detail follows: base reader FUN_1001b4b4 consumes exactly rowCount*rowBytes and nothing else, so the loader totals ~N*N+129 while the saver totals the full section. The loader would read its six trailing u32s at N*N+105, inside the sub-object region - the wrong bytes. The game loads fine, so AT LEAST ONE RECORDED FACT IS WRONG. Candidates: (1) the base read is not N*N (but it cannot be 3*N*N - the u16 permutation is at the END, so 3*N*N lands inside it); (2) the c1 list at N*N is coincidence (against: keys 3000/5000/8000 in all 25 developed files, exact parse); (3) the loader's six u32 reads are not the mirror of the saver's six writes (plausible - the tail is already proven asymmetric); (4) something consumes the region that the decompilation does not show. DO NOT resolve by arithmetic - four inference attempts have failed. Track the stream object's position across the whole loader, or breakpoint under a debugger. Note the bytes just before the count hold N-1 twice (0xff/0xbf/0x7f for N=256/192/128), i.e. grid extents.) | movie tick `FUN_0042a2da` @`0x0042a2da`, draw-frame call `movie->vt[0x10]` | why the intro movie is never ticked | measured windowed: `movie_state_dispatch` 11,468,424 hits, `movie_start_suspend` 1, **`movie_tick` 0**, `movie_stop_resume` 1. The movie goes start -> stop without a single tick, so the first frame draw (`vt+0x10`, called from `FUN_00429f95`) must be returning false, taking the failure path that posts message `(5, 0x1B)` via `FUN_004423b0`. Yet the video IS visibly drawn, so something renders it outside this tick path. Unexplained and possibly related to the half-width defect (U-025) | log the return value of `vt+0x10` in `FUN_00429f95`, and compare tick counts against a fullscreen run | open |
| U-031-RETRACTED | movie tick `FUN_0042a2da` | — | **U-031 (movie tick) IS AN ARTIFACT AND IS WITHDRAWN.** The claimed anomaly "movie_tick = 0 hits while movie_state_dispatch = 11,468,424, so the movie is never ticked" was never measured: the probe log says `FN movie_tick @0x0042A2DA UNDECODABLE prologue 56 8B F1 0F B7 - skipped` and `4/5 instrumented`. The detour never installed, in that run or any other using the same table, so the function's hit count was always 0 regardless of behaviour. Nothing is known about whether the movie ticks. To measure it, teach `prologue_len` the pattern `56 8B F1 0F B7` (push esi; mov esi,ecx; movzx ...) | fix the prologue decoder, then re-measure | withdrawn - never measured |
| U-032 | intro movie state machine `0x00429F54`, state field `+0xAC` | why the movie now starts and never stops | current behaviour (13+ consecutive runs): `movie_start_suspend` 1, `movie_stop_resume` **0**, `movie_post_message` **0**, `movie_state_dispatch` 45,630,950, suspend `+1` with no `-1`, `blt_disp_1` 0. Earlier today the same build family produced `movie_stop_resume` 1, `movie_post_message` 2 and tens of thousands of blits. The movie starts, sets state 1, suspends the renderer, and the stop path never runs, so the renderer stays suspended and nothing draws. **Ruled out as causes:** `Apps\SC3U.exe` SHA-256 still matches the anchor exactly and `GZGraphicD.dll` is untouched; no Maxis/EA registry keys; no SimCity AppData; no game config modified; display mode identical (2560x1440 @ 32bpp) in working and failing runs; disabling all probe surface sampling changed nothing | isolate the remaining probe additions (`g_drawobjs`/`g_srcsurf` collection in `fnlog_enter`, `GetSurfaceDesc` on the primary in `present_hook`); note `re/harness/src` is gitignored so there is NO committed revision to diff against | open - blocks all windowed work |
| U-032-UPDATE | intro movie regression | — | **The probe is EXONERATED.** With every post-`win_blt7` addition gated off (`-sample` default off: surface Lock/IsLost sampling, ColorFill round-trip, lock round-trip, DEST/SRC census collection in `fnlog_enter`), 3/3 runs still show `Resume(-1)` absent and `blt_disp_1 = 0`. Combined with the earlier eliminations this rules out everything under our control: `Apps\SC3U.exe` SHA-256 matches the anchor, `GZGraphicD.dll` untouched, no Maxis/EA registry keys, no SimCity AppData, no game config modified, display mode identical in working and failing runs, trace table restored to the 33 entries that worked in `writer_win.log`, and `Apps\Res\UI\Shared\MOVIES\INTRO.TGQ` present and untouched (25,936,684 bytes, 2000-04-19). **Cause remains unknown.** 16+ consecutive failing runs; the working state existed in `draw_dest.log`, `writer_win.log` and `win_blt7.log` earlier the same day | next session: reboot or a fresh boot of the machine before assuming anything about the game, since every in-repo and per-user explanation has been eliminated; if it recovers on its own, the variable was system-level (driver/DWM state after ~40 TerminateProcess kills) and NOT reproducible from the repo | open - blocks windowed work |
| U-032-RESOLVED | intro movie regression | — | **U-032 IS TRANSIENT AND HAS CLEARED. IT DID NOT NEED A REBOOT.** 3/3 runs on 2026-08-16 late (`recover_A/B/C.log`) show `SUSPEND +1` at `SC3U 0x2A298` followed by `SUSPEND -1` at `SC3U 0x2A35F`, counter back to 0, and `blt_disp_1` = **23,911 / 32,578 / 37,903** (`raster_blit_hw` 23,030 / 31,698 / 37,023) `[CONFIRMED @0x10016bde]`. Same probe build (21:58), same switches, **same boot session** as the 16+ failing runs (last boot 2026-08-15 12:59:43, uptime 1 d 10 h - the prescribed reboot was never performed). The `iso3` and `recover_A` probe banners are line-for-line identical, so nothing under our control differs; the only difference is ~55 min of wall clock. Rule 1 satisfied (`33/33 instrumented`, no `UNDECODABLE`/`UNRECOGNISED` for any counted function), rule 2 satisfied (3 runs, one class). **The transient variable is NOT identified and cannot be bisected after the fact.** Classify any future `+1`-with-no`-1` run as this transient and re-run before concluding anything. See `LAUNCH_CONTROL.md` §16 | windowed work is unblocked; re-measure §15.3 onward from scratch | **resolved - not reproducible, cause unidentified** |
| U-033 | `sc3launch.c:80`, `:144`, `:155`, `:167`, `:214` | — | **Harness defect: relative `-log` / `-gzlog` paths silently do nothing.** The launcher passes both paths verbatim into `SC3PROBE_LOG` / `SC3PROBE_GZLOG` and sets the game's working directory to `<root>\Apps`, so the in-process probe resolves them against `Apps\`, where `re\harness\` does not exist. `-log re\harness\x.log` writes nothing while `sc3launch.c:214` still prints `[*] log written: ...` unconditionally; `-gzlog re\harness\gz_draw.txt` logs `FNLOG: cannot open ... (err 2)` and then **every fnlog counter, `blt_disp_1` included, is absent from the log rather than zero**. The command recorded in `HANDOFF.md` uses relative paths and therefore cannot produce a measurement. `-gzlog` is an INPUT trace table - do not rename or delete `re\harness\gz_draw.txt` | fix: resolve both to absolute paths in `sc3launch.c` before setting the env vars, and make line 214 check the file exists | **FIXED 2026-08-16** - `abspath()` (GetFullPathNameA against the launcher's cwd) now resolves `-log`/`-fnlog`/`-gzlog`; a missing trace table prints an explicit WARNING that its counters will be MISSING not zero; and the "log written" line now stat's the file and prints the byte count, or `NO LOG WRITTEN`. Verified: the relative-path command from HANDOFF now produces `log written: ...\recover_D.log (31048 bytes)` |
| U-034 | `raster_blit_hw` `FUN_10018c58`, present source surface | why the client is black while both the engine and the present are busy | **the present path is EXONERATED**: `present_calls` = `blt_ok` = 1263 (run D t+5s) and 3193 (run E t+10s) with `blt_fail` = **0**, all `DD_OK`, dest `3,32-1003,782`, while `blt_disp_1` = 23,163 and `raster_blit_hw` = 22,283 in the same run `[CONFIRMED]`. Yet the blit source reads `800x600 32bpp 0/7500 LIT`. ~23k engine blits and an empty source surface cannot describe the same buffer, so this is a memory-identity question. The `g_blt_ok`/`g_blt_fail` counters existed but were never printed, and the `engine-fb Blt` log line is capped at `n<=3`, so "3 blits" in every earlier note was a log cap, not a count. See `LAUNCH_CONTROL.md` §17 | log the destination pointer of `raster_blit_hw` and compare it against the present's `src` **within a single run** (the surface is re-allocated per run: `0x03B64F60` run A, `0x03B36110` run D) | open - the current windowed blocker |
